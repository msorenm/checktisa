
# 💎 راهنمای استقرار نهایی سامانه تیسا (Ubuntu Server)

برای اجرای بدون نقص، مراحل زیر را در ترمینال کپی کنید:

### ۱. نصب وابستگی‌ها:
```bash
npm install
```

### ۲. تنظیم API KEY (مهم):
کلید خود را جایگزین `YOUR_KEY` کنید:
```bash
export API_KEY="YOUR_KEY_HERE"
```

### ۳. اجرای برنامه روی پورت ۸۰:
```bash
sudo -E npm run dev
```

### ۴. اجرای دائمی (Background):
اگر می‌خواهید با بستن ترمینال برنامه قطع نشود، از `pm2` استفاده کنید:
```bash
sudo npm install -g pm2
sudo API_KEY="YOUR_KEY" pm2 start "npm run dev" --name tisa
```

**نکته:** اگر صفحه سفید دیدید، مطمئن شوید که دستور `npm install` را کامل انجام داده‌اید.
