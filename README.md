
# 💎 راهنمای جامع نصب و استقرار سامانه تیسا (Tisa Enterprise)
### ویژه سرورهای ابری و اختصاصی Ubuntu (نسخه ۲۰.۰۴ به بالا)

این سند حاوی دستورالعمل‌های دقیق برای راه‌اندازی سامانه روی یک سرور کاملاً خام (Fresh Install) است. تمامی مراحل برای جلوگیری از خطای "صفحه سفید" بهینه‌سازی شده‌اند.

---

## گام ۱: آماده‌سازی و نصب پیش‌نیازها
ابتدا مخازن سیستم را بروزرسانی کرده و Node.js (قلب تپنده سامانه) را نصب می‌کنیم.

```bash
# بروزرسانی کلی سیستم
sudo apt update && sudo apt upgrade -y

# نصب Node.js نسخه 20 (LTS) از منابع معتبر
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs git build-essential

# بررسی نصب صحیح
node -v # باید v20.x باشد
npm -v
```

---

## گام ۲: دریافت سورس کد و نصب وابستگی‌ها
در این مرحله کد را از گیت‌هاب دریافت کرده و تمام کتابخانه‌های مورد نیاز را نصب می‌کنیم.

```bash
# رفتن به پوشه خانگی
cd ~

# کلون کردن پروژه
git clone https://github.com/msorenm/checktisa.git
cd checktisa

# نصب پکیج‌ها (این مرحله حیاتی است)
npm install
```

---

## گام ۳: تنظیمات محیطی (API Key)
سامانه برای کارکرد بخش هوش مصنوعی به کلید گوگل نیاز دارد. این کلید را در متغیرهای سیستم ست می‌کنیم.

```bash
# باز کردن فایل پروفایل برای دائمی کردن کلید
nano ~/.bashrc

# رفتن به انتهای فایل و اضافه کردن خط زیر (کلید خود را جایگزین کنید)
export API_KEY="AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# ذخیره فایل (Ctrl+O و سپس Enter) و خروج (Ctrl+X)
# اعمال تغییرات
source ~/.bashrc
```

---

## گام ۴: مدیریت پورت ۸۰ و فایروال
از آنجایی که پورت ۸۰ حساس است، باید اجازه دسترسی به آن را صادر کنیم.

```bash
# باز کردن پورت‌های مورد نیاز در فایروال اوبونتو
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp
sudo ufw --force enable

# اجازه به Node.js برای استفاده از پورت‌های زیر 1024 بدون نیاز به روت دائمی
sudo setcap 'cap_net_bind_service=+ep' $(which node)
```

---

## گام ۵: استقرار دائمی با PM2 (بدون توقف)
برای اینکه با بستن ترمینال سایت قطع نشود و در صورت ری‌بوت سرور، سایت خودکار بالا بیاید:

```bash
# نصب مدیریت پروسه PM2 به صورت سراسری
sudo npm install -g pm2

# شروع به کار سامانه (تزریق کلید مستقیم برای اطمینان)
API_KEY="کلید_شما" pm2 start "npm run dev" --name tisa-app

# تنظیم اجرای خودکار در هنگام بوت سرور
pm2 startup systemd
# دستوری که خروجی مرحله قبل به شما می‌دهد را کپی و اجرا کنید
pm2 save
```

---

## 🛠 رفع مشکلات احتمالی (Troubleshooting)

### ۱. اگر هنوز صفحه سفید می‌بینید:
پوشه بیلد را پاک کرده و دوباره نصب کنید تا کش‌های قدیمی حذف شوند:
```bash
rm -rf node_modules package-lock.json
npm install
```

### ۲. بررسی لاگ‌های سیستم:
اگر سایت بالا نمی‌آید، دلیل آن را در لاگ‌های PM2 ببینید:
```bash
pm2 logs tisa-app
```

### ۳. تداخل پورت ۸۰:
اگر برنامه دیگری (مثل آپاچی یا انجین‌اکس) روی پورت ۸۰ باشد، سامانه اجرا نمی‌شود. آن را متوقف کنید:
```bash
sudo systemctl stop nginx || sudo systemctl stop apache2
```

---

## 💡 نکات امنیتی مهندسی
*   **SSL:** پیشنهاد می‌شود در آینده یک دامنه تهیه کرده و با `Certbot` گواهینامه SSL رایگان نصب کنید.
*   **Backup:** فایل `tisa_checks` در `localStorage` مرورگر ذخیره می‌شود. برای امنیت بیشتر، از دکمه "دریافت فایل JSON" در بخش تنظیمات برای بکاپ‌گیری دوره‌ای استفاده کنید.

**توسعه یافته با ❤️ برای اکوسیستم مالی ایران**
