
# 💎 راهنمای راه‌اندازی سامانه هوشمند تیسا (Tisa)

این پروژه با معماری **Modern ESM** طراحی شده است. به این معنی که کتابخانه‌ها مستقیماً از CDN بارگذاری می‌شوند و شما نیازی به نصب هزاران پکیج در `node_modules` ندارید.

---

## 🛠 مرحله ۱: آماده‌سازی فایل‌ها
ابتدا مطمئن شوید تمام فایل‌های زیر در یک پوشه (مثلاً به نام `tisa-app`) قرار دارند:
- `index.html`
- `index.tsx`
- `App.tsx`
- `types.ts`
- `constants.tsx`
- پوشه `pages/` (شامل تمام صفحات)
- پوشه `services/` (شامل سرویس‌های Gemini و تلگرام)

---

## 🚀 مرحله ۲: اجرای پروژه (بدون نیاز به نصب پکیج)

بهترین و سریع‌ترین راه برای اجرای این پروژه، استفاده از یک Local Server ساده است. 

### اگر Node.js نصب دارید:
کافیست در ترمینال (Terminal/Command Prompt) وارد پوشه پروژه شده و دستور زیر را بزنید:

```bash
# اجرای مستقیم سرور بدون نصب
npx serve .
```
سپس آدرس `http://localhost:3000` را در مرورگر باز کنید.

### اگر می‌خواهید با VS Code اجرا کنید:
1. افزونه **Live Server** را نصب کنید.
2. روی فایل `index.html` راست کلیک کرده و **Open with Live Server** را بزنید.

---

## 🔑 مرحله ۳: تنظیم API Key هوش مصنوعی

برای اینکه بخش‌های AI (مثل تحلیل چک و رادار پیش‌گو) کار کنند:
1. وارد [Google AI Studio](https://aistudio.google.com/) شوید و یک **API Key** بسازید.
2. در محیطی که پروژه را اجرا می‌کنید (مثل Vercel یا ادیتور خود)، متغیر محیطی با نام `API_KEY` را ست کنید.
3. **نکته:** اگر به صورت محلی تست می‌کنید، سیستم به صورت خودکار از متغیرهای سیستم شما استفاده می‌کند.

---

## 🤖 مرحله ۴: تنظیمات ربات تلگرام

برای دریافت هشدارها روی موبایل:
1. در تلگرام به `@BotFather` پیام دهید و یک ربات بسازید تا **Token** بگیرید.
2. به بخش **تنظیمات** در داخل اپلیکیشن تیسا بروید.
3. توکن ربات و **Chat ID** خود را وارد کرده و دکمه "تست اتصال" را بزنید.

---

## 🖥 زیرساخت و سرور پیشنهادی برای استقرار (Deployment)

از آنجایی که این یک اپلیکیشن **Client-side** است، بهترین گزینه‌ها عبارتند از:

### ۱. پیشنهاد ویژه: Vercel (رایگان و پرقدرت)
- پوشه پروژه را در GitHub آپلود کنید.
- در پنل Vercel پروژه را Import کنید.
- در بخش **Environment Variables**، مقدار `API_KEY` را وارد کنید.
- تمام! پروژه با SSL رایگان بالا می‌آید.

### ۲. سرور شخصی (Ubuntu/CentOS)
اگر از **Nginx** استفاده می‌کنید، این فایل کانفیگ را کپی کنید:

```nginx
server {
    listen 80;
    server_name tisa-check.ir; # دامنه شما
    root /var/www/html/tisa; # مسیر فایل‌های پروژه
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # رفع مشکل لود نشدن ماژول‌های TSX در برخی سرورها
    location ~* \.(tsx|ts)$ {
        add_header Content-Type application/javascript;
    }
}
```

---

## ❓ چرا خطای "Folder not found" می‌گیرید؟
اگر دستور `cd tisa-enterprise` کار نمی‌کند، به این دلیل است که نام پوشه شما چیز دیگری است. 
- در ویندوز: داخل پوشه پروژه در نوار آدرس بالا تایپ کنید `cmd` و اینتر بزنید. مستقیماً در همان مسیر باز می‌شود.
- در مک/لینوکس: پوشه را بکشید و داخل ترمینال رها کنید تا مسیر دقیق را بنویسد.

> **تیسا؛ جایی که امنیت بانکی با هوش مصنوعی ملاقات می‌کند.**
