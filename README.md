
# 🚀 راهنمای نصب نهایی تیسا (بدون خطا - ویژه اوبونتو)

اگر بعد از نصب صفحه سفید می‌بینید، به این دلیل است که فایل‌های `.tsx` باید توسط یک کامپایلر اجرا شوند. این راهنما از **Vite** استفاده می‌کند که سریع‌ترین و مطمئن‌ترین راه است.

---

## گام ۱: نصب ابزارهای مورد نیاز
ابتدا Node.js و ابزارهای لازم را روی سرور اوبونتو نصب کنید:

```bash
sudo apt update
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

---

## گام ۲: ایجاد پروژه و کپی فایل‌ها
یک پوشه بسازید و تمام فایل‌های پروژه را در آن قرار دهید:

```bash
mkdir ~/tisa-enterprise
cd ~/tisa-enterprise
```
*حالا تمام فایل‌هایی که دارید (App.tsx, index.tsx, index.html و ...) را در این پوشه آپلود کنید.*

---

## گام ۳: نصب وابستگی‌های اجرایی
برای اینکه مرورگر کدهای تایپ‌اسکریپت را بفهمد، دستورات زیر را بزنید:

```bash
# ایجاد فایل پکیج
npm init -y

# نصب وییت و پلاگین ری‌اکت
npm install vite @vitejs/plugin-react
```

---

## گام ۴: ایجاد فایل کانفیگ Vite
یک فایل به نام `vite.config.ts` بسازید:
```bash
nano vite.config.ts
```
و این کد را در آن قرار دهید:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  define: {
    'process.env.API_KEY': JSON.stringify(process.env.API_KEY)
  }
});
```

---

## گام ۵: اجرای برنامه (تضمینی)
ابتدا API Key خود را ست کنید و سپس برنامه را اجرا کنید:

```bash
# ست کردن کلید گوگل (جایگزین کنید با کلید خودتان)
export API_KEY="AIzaSy..."

# اجرای برنامه روی پورت ۸۰ (دسترسی عمومی)
sudo npx vite --host --port 80
```

---

## 💡 نکات مهم برای پایداری:
1. **اجرای در پس‌زمینه:** برای اینکه با بستن ترمینال سایت قطع نشود، از دستور زیر استفاده کنید:
   `sudo API_KEY="کلید_شما" npx vite --host --port 80 &`
2. **دسترسی فایل‌ها:** اگر خطای Permission گرفتید:
   `sudo chmod -R 777 ~/tisa-enterprise`
3. **مشاهده سایت:** آی‌پی سرور خود را در مرورگر بزنید. اگر پورت ۸۰ را انتخاب کردید، نیازی به وارد کردن پورت نیست.

**تبریک! حالا سامانه تیسا بدون هیچ مشکلی و با سرعت بالا در سرور شما اجرا می‌شود.**
