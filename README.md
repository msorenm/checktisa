
# 💎 سامانه هوشمند مدیریت چک تیسا (Tisa Enterprise)

**تیسا** یک پلتفرم فوق‌پیشرفته و تحت استانداردهای بانکی برای مدیریت زنجیره چک، تحلیل نقدینگی و پیش‌بینی بحران‌های مالی با بهره‌گیری از هوش مصنوعی **Google Gemini 3 Pro** است.

---

## 🚀 نقشه راه نصب و راه‌اندازی سریع

### ۱. پیش‌نیازهای سیستمی
برای اجرای بهینه سامانه، اطمینان حاصل کنید که ابزارهای زیر را در اختیار دارید:
*   **Node.js**: نسخه ۱۸ یا بالاتر.
*   **مرورگر مدرن**: Chrome یا Edge (جهت پشتیبانی از قابلیت‌های هوش مصنوعی صوتی).
*   **API Key**: دریافت کلید از [Google AI Studio](https://aistudio.google.com/).

### ۲. نصب گام‌به‌گام
ابتدا پروژه را در محیط محلی خود کپی کرده و مراحل زیر را دنبال کنید:

```bash
# ۱. کلون کردن مخزن (یا دانلود فایل‌ها)
git clone https://github.com/your-repo/tisa-enterprise.git

# ۲. ورود به پوشه پروژه
cd tisa-enterprise

# ۳. نصب بسته‌های مورد نیاز (در صورت استفاده از مدیریت پکیج)
npm install
```

### ۳. تنظیمات متغیرهای محیطی (Environment Variables)
سامانه تیسا برای برقراری ارتباط با مدل‌های هوش مصنوعی به یک کلید معتبر نیاز دارد. یک فایل `.env` در ریشه پروژه ایجاد کرده و کد زیر را در آن قرار دهید:

```env
API_KEY=your_gemini_api_key_here
```

---

## 🛠 تنظیمات اختصاصی بخش‌ها

### الف) اتصال به تلگرام (Telegram Integration)
برای دریافت اعلانات آنی و پادکست‌های استراتژیک صوتی:
1.  به ربات `@BotFather` در تلگرام مراجعه کرده و یک ربات جدید بسازید.
2.  **Bot Token** دریافتی را در بخش تنظیمات اپلیکیشن وارد کنید.
3.  **Chat ID** خود یا گروه مدیریتی را (با استفاده از ربات‌هایی مثل `@userinfobot`) استخراج و ثبت کنید.

### ب) فعال‌سازی رادار پیش‌گو (Forecaster)
این بخش نیاز به حداقل ۳ چک ثبت شده با تاریخ‌های متفاوت دارد تا بتواند نمودار جریان نقدینگی و بحران‌های احتمالی را با دقت بالا شبیه‌سازی کند.

---

## 🖥 زیرساخت و سرور پیشنهادی (Deployment)

با توجه به معماری **Single Page Application (SPA)** و استفاده از ماژول‌های **ES6**، گزینه‌های زیر برای میزبانی پیشنهاد می‌شود:

### ۱. میزبانی ابری (پیشنهادی برای سرعت بالا)
*   **Vercel / Netlify**: بهترین گزینه برای استقرار سریع. فایل `index.html` را به عنوان Entry Point شناسایی می‌کنند.
*   **تنظیمات سرور**: باید تمامی درخواست‌ها به `index.html` روت شوند (Fallback Strategy).

### ۲. سرور اختصاصی (Nginx / Apache)
اگر قصد دارید سامانه را روی سرور شخصی (Ubuntu/CentOS) میزبانی کنید، نمونه کانفیگ **Nginx** به شرح زیر است:

```nginx
server {
    listen 80;
    server_name tisa.yourdomain.com;
    root /var/www/tisa;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # بهینه‌سازی برای ماژول‌های ES6
    location ~* \.(js|mjs|ts|tsx)$ {
        add_header Content-Type application/javascript;
    }
}
```

---

## 🛡 امنیت و حریم خصوصی
*   **Local Storage Encryption**: داده‌های چک‌ها در مرورگر کاربر به صورت رمزنگاری شده ذخیره می‌شوند.
*   **Gemini Privacy**: داده‌های ارسالی به Gemini صرفاً جهت پردازش بوده و طبق تنظیمات API Key، در مدل‌های عمومی آموزش داده نمی‌شوند.
*   **2FA**: سیستم ورود دارای تایید دو مرحله‌ای شبیه‌سازی شده برای امنیت حداکثری اپراتور است.

---

## 📞 پشتیبانی فنی
در صورت بروز هرگونه مشکل در فرآیند نصب یا تنظیمات API، می‌توانید از طریق بخش **Gemini Lab** مستقیماً با هوش مصنوعی مستقر در سامانه مشورت کنید.

> **تیسا؛ نگهبان هوشمند نقدینگی شما.**
