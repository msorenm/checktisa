
# 💎 راهنمای استقرار سامانه تیسا روی سرور Ubuntu

این راهنما برای نصب سامانه روی سرور لینوکس (اوبونتو ۲۲.۰۴ یا بالاتر) به صورت گام‌به‌گام تهیه شده است.

---

## ۱. نصب پیش‌نیازها (Node.js & Nginx)
ابتدا مخازن را آپدیت کرده و ابزارهای لازم را نصب کنید:

```bash
# آپدیت مخازن
sudo apt update

# نصب Node.js (نسخه ۲۰)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# نصب Nginx برای میزبانی وب
sudo apt install -y nginx
```

---

## ۲. ایجاد پوشه پروژه و انتقال فایل‌ها
فرض می‌کنیم شما می‌خواهید پروژه را در پوشه `tisa-app` قرار دهید:

```bash
# ایجاد پوشه در مسیر Home
mkdir -p ~/tisa-app
cd ~/tisa-app
```

**نکته:** تمام فایل‌های پروژه (index.html, App.tsx, index.tsx و پوشه‌های pages/services) را در این مسیر (`~/tisa-app`) کپی کنید. اگر از Git استفاده می‌کنید:
```bash
git clone <URL_PROJECT> .
```

---

## ۳. تنظیم API Key (بسیار مهم)
برای اینکه هوش مصنوعی Gemini کار کند، باید کلید خود را در سیستم تعریف کنید. 

### روش موقت (برای تست):
```bash
export API_KEY="کلید_شما_اینجا"
npx serve .
```

### روش دائمی (در پروفایل کاربر):
```bash
echo 'export API_KEY="کلید_شما_اینجا"' >> ~/.bashrc
source ~/.bashrc
```

---

## ۴. راه‌اندازی دائمی با Nginx (Production)
برای اینکه سایت همیشه بالا باشد و با بستن ترمینال قطع نشود، از Nginx استفاده می‌کنیم.

۱. فایل پیکربندی جدید بسازید:
```bash
sudo nano /etc/nginx/sites-available/tisa
```

۲. محتویات زیر را کپی و در فایل قرار دهید (آدرس `/home/USER/tisa-app` را با یوزر خودتان جایگزین کنید):
```nginx
server {
    listen 80;
    server_name YOUR_SERVER_IP_OR_DOMAIN;

    root /home/YOUR_USERNAME/tisa-app;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # پشتیبانی از ماژول‌های مدرن
    location ~* \.(tsx|ts|js|mjs)$ {
        add_header Content-Type application/javascript;
    }

    error_page 404 /index.html;
}
```

۳. فعال‌سازی تنظیمات:
```bash
sudo ln -s /etc/nginx/sites-available/tisa /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## ۵. عیب‌یابی (Troubleshooting)

### اگر پوشه پیدا نشد:
دستور `pwd` را بزنید تا مطمئن شوید در مسیر درست هستید. نام پوشه باید دقیقاً همان چیزی باشد که ساخته‌اید.

### اگر فایل‌ها لود نمی‌شوند:
مطمئن شوید دسترسی (Permission) فایل‌ها برای Nginx باز است:
```bash
chmod -R 755 ~/tisa-app
```

### تست سریع بدون Nginx:
اگر فقط می‌خواهید سریع چک کنید که پروژه کار می‌کند:
```bash
cd ~/tisa-app
npx serve -l 8080 .
```
سپس در مرورگر آدرس `IP_SERVER:8080` را باز کنید.

---

**تیسا؛ قدرت‌یافته از هوش مصنوعی برای امنیت مالی شما.**
